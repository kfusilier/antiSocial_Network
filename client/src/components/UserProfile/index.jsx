import style from './userProfile.module.css';

const UserProfile = () => {
	return (
		<div className={style.userProfile}>
			<p>Name:</p>
			<p>UserName:</p>
		</div>
	);
};

export default UserProfile;
